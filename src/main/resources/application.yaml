#-----------------------------------
--- # Application
#-----------------------------------
spring:
  application:
    name: thesis-webrepo
server:
  port: 8080
  servlet:
    context-path: /api
--- ## File upload limit
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 500KB
      max-request-size: 500KB

#-----------------------------------
--- # Database
#-----------------------------------
spring:
  jpa:
    open-in-view: false
  h2:
    console:
      enabled: false

# --- # PostgreSQL
# spring:
#   jpa:
#     hibernate:
#       ddl-auto: validate
#   ## Datasource
#   datasource:
#     url: jdbc:postgresql://localhost:5432/webrepo
#     username: 's53kr_lm8a6r'
#     password: '$p@s5w0rD_s53k12x'

--- # H2 In-Memory
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  ## Datasource
  datasource:
    url: jdbc:h2:mem:testdb
    username: 's53kr_lm8a6r'
    password: '$p@s5w0rD_s53k12x'

#-----------------------------------
--- # Security
#-----------------------------------
application:
  security:
    # DEFINE ROUNDS OF VALIDATION
    # TO STRENGTHEN PASSWORD PROCESSING
    strength: 16
    # DEFAULT LOGIN CREDENTIALS
    # WITH ROLE OF SYSTEM ADMIN
    default-login-enabled: true
    default-login: sysadm
    default-password: sysadm

#-----------------------------------
--- # Storage Service (MinIO)
#-----------------------------------
application:
  minio:
    url: cnurl
    username: uname
    password: paswd
